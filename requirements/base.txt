# ===========================================================================
# AgileIQ — requirements/base.txt
# Dépendances RUNTIME communes à TOUS les environnements (dev, test, prod).
# Versions épinglées pour la reproductibilité (principe production-ready).
# ===========================================================================

# --- Framework web --------------------------------------------------------
Flask==3.1.0
Werkzeug==3.1.3                 # serveur WSGI de dev + utilitaires (lié à Flask)

# --- ORM & migrations -----------------------------------------------------
SQLAlchemy==2.0.36              # ORM, API 2.0 (typage moderne)
Flask-SQLAlchemy==3.1.1         # intégration SQLAlchemy <-> contexte Flask
alembic==1.14.0                 # moteur de migrations de schéma
Flask-Migrate==4.0.7            # intégration Alembic <-> Flask (commandes CLI)
psycopg[binary]==3.2.3          # driver PostgreSQL (psycopg 3, binaire)

# --- Validation & sérialisation (DTO / schémas) ---------------------------
marshmallow==3.23.1             # sérialisation / validation des entrées-sorties
flask-smorest==0.45.0           # blueprints REST + génération OpenAPI/Swagger
apispec==6.8.1                  # spécification OpenAPI

# --- Sécurité / authentification ------------------------------------------
Flask-JWT-Extended==4.7.1       # gestion des tokens JWT (access / refresh)
passlib[bcrypt]==1.7.4          # hachage des mots de passe (bcrypt)
Flask-Cors==5.0.0               # gestion du CORS (frontend React séparé)
Flask-Limiter==3.9.2            # rate limiting (protection des endpoints)

# --- Tâches asynchrones ---------------------------------------------------
celery==5.4.0                   # workers asynchrones (sync GitLab, ML batch...)
redis==5.2.1                    # broker Celery + cache + rate limiting

# --- Configuration & environnement ----------------------------------------
pydantic==2.10.4                # validation typée de la configuration
pydantic-settings==2.7.0        # chargement config depuis variables d'env
python-dotenv==1.0.1            # lecture du fichier .env en développement

# --- Observabilité --------------------------------------------------------
structlog==24.4.0               # logs structurés JSON
prometheus-flask-exporter==0.23.1  # exposition des métriques Prometheus

# --- Utilitaires HTTP -----------------------------------------------------
requests==2.32.3                # client HTTP (intégrations GitLab / Taiga)
tenacity==9.0.0                 # retry / backoff (résilience des appels externes)
